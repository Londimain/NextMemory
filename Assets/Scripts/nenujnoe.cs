using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class nenujnoe : MonoBehaviour
{
//MoneyManager
/*
//загрузка сцены
public string nameScene = "";

public void RestartGame() { 
    SceneManager.LoadScene(nameScene)
}*/


/*
// Сохранение значения количества колечек
int collectedRings = 10;
PlayerPrefs.SetInt("CollectedRings", collectedRings);
PlayerPrefs.Save();
// Получение сохраненного значения количества колечек
int collectedRings = PlayerPrefs.GetInt("CollectedRings");
[System.Serializable]
public class GameData
{
    public int collectedRings;
}

// Сохранение состояния игры
GameData data = new GameData();
data.collectedRings = 10;

string jsonData = JsonUtility.ToJson(data);
File.WriteAllText("save.json", jsonData);

// Восстановление состояния игры
string jsonData = File.ReadAllText("save.json");
GameData data = JsonUtility.FromJson<GameData>(jsonData);

int collectedRings = data.collectedRings;
*/


//using UnityEngine.Random = System.Random;
//UnityEngine.Random.Range(0,5);
}
